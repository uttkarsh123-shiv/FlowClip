{
  "name": "project-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/extension",
    "convex",
    "shared"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:convex": "npm run dev --workspace=convex"
  }
}
