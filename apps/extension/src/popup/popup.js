// Popup UI logic
document.addEventListener("DOMContentLoaded", () => {
  const container = document.getElementById("clips");
  // TODO: fetch clips from Convex and render
  container.textContent = "No clips yet.";
});
